services:
  prompt-router:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    environment:
      - LOG_FILE=/app/logs/route_log.jsonl
    restart: unless-stopped
